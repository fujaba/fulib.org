import { SolutionNamePipe } from './solution-name.pipe';

describe('SolutionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SolutionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
