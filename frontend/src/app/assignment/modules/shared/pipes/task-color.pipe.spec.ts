import { TaskColorPipe } from './task-color.pipe';

describe('TaskColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskColorPipe();
    expect(pipe).toBeTruthy();
  });
});
