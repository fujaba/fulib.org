import { GithubLinkPipe } from './github-link.pipe';

describe('GithubLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GithubLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
